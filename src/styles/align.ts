import tw from 'twin.macro';

export const horizontal_mid = tw`flex flex-direction[column] align-items[center]`;
export const vertical_mid = tw`flex flex-direction[column] justify-content[center]`;
export const mid = tw`flex flex-direction[row] justify-content[center] align-items[center]`;
